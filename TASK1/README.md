<!-- # Editable Task Table Using ReactJS

This project creates a simple **editable task table** using **ReactJS**, integrated into an HTML page.

## How to Run

1. Open `index.html` in a modern browser.
2. The task table will load, allowing edits in each column.
3. Click **Submit Data** to log the edited data in the console.

---

## Files

- **index.html**: Contains the HTML structure and React integration.
- **app.js**: Contains ReactJS logic for rendering and managing the editable table.

---

## Technologies Used

- **ReactJS**: For creating the interactive table.
- **HTML**: For the base structure.
- **Tailwind CSS**: For styling.

---

## Features

- **Editable Table**: A table with predefined tasks that can be edited.
- **Real-time Data Entry**: Six fields per task can be edited in real-time.
- **Submit Button**: A button that prepares and logs the data for API submission.

---

## Notes

- No **Node.js** is required for this project.
- React is integrated via `<script>` tags, as per the [React Docs](https://reactjs.org/docs/add-react-to-a-website.html).

---

## Author

This project demonstrates creating an editable table using ReactJS and is aimed at showcasing basic React integration with an HTML page. -->
